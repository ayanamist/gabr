<?xml version="1.0" encoding="UTF-8" ?>{% import "render.html" as render with context %}
<rss version="2.0">
    <channel>
        <title>Twitter Home</title>
        <description>Twitter home timeline exported as a RSS feed.</description>
        <link>{{ request.host_url }}</link>
        <lastBuildDate>{{ now }}</lastBuildDate>
        <pubDate>{{ results|first|item("created_at") }}</pubDate>
        <ttl>60</ttl>
        {% for result in results %}
            <item>
                <title>@{{ result.user.screen_name }}: {{ result.text }}</title>
                <description><![CDATA[
                        {{ render.render_tweet(result) }}]]></description>
                <link>{{ abs_url_for("status", id=result.id) }}#t{{ result.id }}</link>
                <guid>{{ abs_url_for("status", id=result.id) }}#t{{ result.id }}</guid>
                <pubDate>{{ result.created_at }}</pubDate>
            </item>
        {% endfor %}
    </channel>
</rss>